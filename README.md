# 🐱 یادآور غذای گربه - PWA

یک اپلیکیشن وب پیشرو (PWA) برای مدیریت و یادآوری غذای گربه‌های شما با رابط کاربری فارسی.

## ✨ ویژگی‌ها

- **پروفایل چندگانه**: مدیریت چندین گربه با پروفایل‌های جداگانه
- **محاسبه هوشمند**: محاسبه مقدار غذا بر اساس وزن، سن و سطح فعالیت
- **پیشنهاد غذایی**: پیشنهاد نوع و مقدار غذا (خشک، تر یا ترکیبی)
- **زمان‌بندی**: برنامه‌ریزی وعده‌های غذایی در ساعات مختلف روز
- **نوتیفیکیشن**: یادآوری خودکار زمان غذا دادن
- **PWA**: قابلیت نصب روی موبایل و دسکتاپ
- **ذخیره‌سازی امن**: استفاده از IndexedDB برای ذخیره‌سازی محلی
- **صادر/وارد**: امکان پشتیبان‌گیری و بازیابی داده‌ها

## 🚀 نصب و راه‌اندازی

### نصب محلی

1. کلون کردن پروژه:
```bash
git clone https://github.com/Pxrvlz/cat-food-reminder-pwa.git
cd cat-food-reminder-pwa
```

2. استفاده از یک سرور محلی (برای تست PWA):
```bash
# با Python
python -m http.server 8000

# با Node.js (http-server)
npx http-server -p 8000

# با PHP
php -S localhost:8000
```

3. باز کردن در مرورگر:
```
http://localhost:8000
```

## 📱 نصب به عنوان اپلیکیشن

### در موبایل:
1. باز کردن سایت در مرورگر
2. انتخاب "افزودن به صفحه اصلی" از منوی مرورگر
3. یا استفاده از دکمه "نصب اپلیکیشن" در هدر

### در دسکتاپ (Chrome/Edge):
1. کلیک روی آیکون نصب در نوار آدرس
2. یا استفاده از دکمه "نصب اپلیکیشن" در هدر

## 🛠️ فناوری‌های استفاده شده

- HTML5
- CSS3 (با پشتیبانی RTL)
- JavaScript (ES6+)
- IndexedDB (ذخیره‌سازی محلی)
- Service Worker (PWA)
- Web Notifications API

## 📊 محاسبه مقدار غذا

اپلیکیشن از فرمول‌های علمی برای محاسبه نیاز کالری روزانه استفاده می‌کند:

- **RER (Resting Energy Requirement)**: `70 × (وزن به کیلوگرم)^0.75`
- **MER (Maintenance Energy Requirement)**: `RER × ضریب فعالیت × ضریب سن`

### ضریب فعالیت:
- کم: 1.0 (گربه‌های خانگی)
- متوسط: 1.2
- زیاد: 1.4 (گربه‌های فعال)

### ضریب سن:
- کمتر از 1 سال: 1.5
- 1-2 سال: 1.2
- 2-10 سال: 1.0
- بیشتر از 10 سال: 0.9

### کالری غذا:
- غذاهای خشک: ~375 کیلوکالری در 100 گرم
- غذاهای تر: ~90 کیلوکالری در 100 گرم

## 📁 ساختار پروژه

```
cat-food-reminder-pwa/
├── index.html          # صفحه اصلی
├── app.js              # منطق اپلیکیشن
├── styles.css          # استایل‌ها
├── manifest.json       # تنظیمات PWA
├── service-worker.js   # Service Worker
├── icon-192.png        # آیکون 192x192
├── icon-512.png        # آیکون 512x512
├── .gitignore
├── README.md
└── .github/
    └── workflows/
        └── deploy.yml  # GitHub Actions برای دیپلوی
```

## 🌐 دیپلوی در GitHub Pages

### روش 1: استفاده از GitHub Actions (پیشنهادی)

1. فایل `.github/workflows/deploy.yml` از قبل ایجاد شده است
2. رفتن به Settings > Pages در مخزن GitHub
3. انتخاب "GitHub Actions" به عنوان source
4. با هر push به branch `main` یا `master`، اپلیکیشن به صورت خودکار دیپلوی می‌شود

### روش 2: دیپلوی دستی

1. رفتن به Settings > Pages در مخزن GitHub
2. انتخاب branch `main` یا `master` و پوشه `/ (root)` به عنوان source
3. کلیک روی Save

### تنظیم مسیرها

اگر repository شما نام متفاوتی دارد یا می‌خواهید در root دیپلوی کنید:

1. **برای subdirectory** (پیش‌فرض): 
   - `manifest.json` از قبل تنظیم شده است
   - اگر نام repository متفاوت است، `start_url` و `scope` را در `manifest.json` تغییر دهید

2. **برای root deployment** (username.github.io):
   - در `manifest.json` تغییر دهید:
   ```json
   "start_url": "/",
   "scope": "/"
   ```

### ایجاد آیکون‌ها

قبل از دیپلوی، باید آیکون‌ها را ایجاد کنید:

1. باز کردن `create-icons.html` در مرورگر
2. کلیک روی دکمه‌های "دانلود آیکون"
3. فایل‌های `icon-192.png` و `icon-512.png` را در root پروژه ذخیره کنید

پس از دیپلوی، اپلیکیشن در آدرس زیر در دسترس خواهد بود:
```
https://yourusername.github.io/cat-food-reminder-pwa/
```
یا اگر در root دیپلوی شده:
```
https://yourusername.github.io/
```

## 🔒 حریم خصوصی

- تمام داده‌ها به صورت محلی در مرورگر شما ذخیره می‌شوند
- هیچ داده‌ای به سرور ارسال نمی‌شود
- امکان صادر کردن داده‌ها برای پشتیبان‌گیری

## 📝 مجوز

این پروژه تحت مجوز MIT منتشر شده است.

## 🤝 مشارکت

مشارکت‌ها، پیشنهادات و گزارش باگ‌ها خوش‌آمد هستند!

## 📧 تماس

برای سوالات و پیشنهادات، لطفاً یک Issue ایجاد کنید.

---

ساخته شده با ❤️ برای دوستداران گربه‌ها

