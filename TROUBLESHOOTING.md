# راهنمای عیب‌یابی

## مشکل: دکمه نصب نمایش داده نمی‌شود

### دلایل احتمالی:

1. **PWA قبلاً نصب شده است**
   - اگر اپلیکیشن قبلاً نصب شده باشد، دکمه نصب نمایش داده نمی‌شود
   - برای بررسی: در Chrome/Edge به `chrome://apps` بروید

2. **استفاده از HTTP به جای HTTPS**
   - PWA فقط روی HTTPS کار می‌کند
   - **راه حل**: در localhost هم کار می‌کند، اما برای production باید HTTPS باشد
   - GitHub Pages به صورت خودکار HTTPS دارد

3. **Manifest.json معتبر نیست**
   - بررسی کنید که فایل `manifest.json` به درستی لود می‌شود
   - در Developer Tools > Application > Manifest بررسی کنید
   - مطمئن شوید که آیکون‌ها وجود دارند

4. **Service Worker ثبت نشده**
   - در Developer Tools > Application > Service Workers بررسی کنید
   - باید Service Worker به صورت "activated" باشد

5. **مرورگر از PWA پشتیبانی نمی‌کند**
   - Chrome, Edge, Safari iOS, Firefox (محدود) پشتیبانی می‌کنند
   - در Safari macOS باید از Chrome یا Edge استفاده کنید

### راه‌حل:

1. Console مرورگر را باز کنید (F12)
2. بررسی کنید که آیا خطایی وجود دارد
3. در Developer Tools > Application > Manifest بررسی کنید
4. مطمئن شوید که Service Worker ثبت شده است

## مشکل: نوتیفیکیشن کار نمی‌کند

### دلایل احتمالی:

1. **Permission داده نشده**
   - **راه حل**: 
     - به تب "تنظیمات" بروید
     - روی دکمه "درخواست دسترسی نوتیفیکیشن" کلیک کنید
     - یا از دکمه "ارسال تست نوتیفیکیشن" استفاده کنید

2. **Service Worker ثبت نشده**
   - Service Worker برای نوتیفیکیشن ضروری است
   - در Developer Tools > Application > Service Workers بررسی کنید

3. **Permission مسدود شده**
   - اگر قبلاً permission را رد کرده‌اید:
     - **Chrome/Edge**: Settings > Privacy > Site Settings > Notifications
     - **Firefox**: Settings > Privacy > Permissions > Notifications
     - سایت را پیدا کرده و permission را reset کنید

4. **مرورگر از Notification API پشتیبانی نمی‌کند**
   - Chrome, Edge, Firefox, Safari (محدود) پشتیبانی می‌کنند
   - در Safari iOS باید از Chrome یا Edge استفاده کنید

5. **استفاده از HTTP**
   - Notification API فقط روی HTTPS کار می‌کند
   - در localhost هم کار می‌کند

### راه‌حل گام به گام:

1. **بررسی Permission:**
   ```javascript
   // در Console مرورگر اجرا کنید:
   console.log(Notification.permission);
   // باید "granted" باشد
   ```

2. **بررسی Service Worker:**
   - Developer Tools > Application > Service Workers
   - باید Service Worker به صورت "activated" باشد

3. **تست دستی:**
   - به تب "تنظیمات" بروید
   - روی "ارسال تست نوتیفیکیشن" کلیک کنید
   - اگر کار کرد، مشکل از زمان‌بندی است

4. **بررسی Console:**
   - Console را باز کنید (F12)
   - خطاها را بررسی کنید

## مشکل: آیکون‌ها نمایش داده نمی‌شوند

### راه‌حل:

1. فایل `create-icons.html` را در مرورگر باز کنید
2. آیکون‌ها را دانلود کنید
3. فایل‌های `icon-192.png` و `icon-512.png` را در root پروژه ذخیره کنید
4. صفحه را refresh کنید

## مشکل: Service Worker ثبت نمی‌شود

### دلایل:

1. **استفاده از HTTP (به جز localhost)**
   - Service Worker فقط روی HTTPS کار می‌کند
   - در localhost هم کار می‌کند

2. **مسیر Service Worker اشتباه است**
   - بررسی کنید که `service-worker.js` در root پروژه است
   - مسیر در `app.js` باید `'./service-worker.js'` باشد

3. **خطا در Service Worker**
   - Console را بررسی کنید
   - در Developer Tools > Application > Service Workers خطاها را ببینید

### راه‌حل:

1. Console را باز کنید
2. خطاهای Service Worker را بررسی کنید
3. Service Worker را unregister کنید:
   - Developer Tools > Application > Service Workers
   - روی "Unregister" کلیک کنید
4. صفحه را refresh کنید

## تست سریع

برای تست سریع همه چیز:

1. **تست PWA:**
   - Console را باز کنید
   - بررسی کنید که Service Worker ثبت شده است
   - بررسی کنید که Manifest لود شده است

2. **تست نوتیفیکیشن:**
   - به تنظیمات بروید
   - روی "ارسال تست نوتیفیکیشن" کلیک کنید
   - باید یک نوتیفیکیشن نمایش داده شود

3. **تست نصب:**
   - اگر دکمه نصب نمایش داده نمی‌شود:
     - در Chrome/Edge: آیکون نصب در نوار آدرس را بررسی کنید
     - یا از منوی مرورگر استفاده کنید

## نکات مهم

- ⚠️ **HTTPS ضروری است** (به جز localhost)
- 📱 **تست روی موبایل**: حتماً روی موبایل هم تست کنید
- 🔄 **Clear Cache**: بعد از تغییرات، cache را پاک کنید
- 🧪 **Developer Tools**: همیشه Console را بررسی کنید

## درخواست کمک

اگر مشکل حل نشد:

1. Console را باز کنید و خطاها را کپی کنید
2. در Developer Tools > Application وضعیت را بررسی کنید
3. یک Issue در GitHub ایجاد کنید و اطلاعات را ارسال کنید

