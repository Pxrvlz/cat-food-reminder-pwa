# راهنمای دیپلوی در GitHub Pages

## مراحل دیپلوی

### 1. ایجاد آیکون‌ها

قبل از دیپلوی، باید آیکون‌های PWA را ایجاد کنید:

**روش 1: استفاده از HTML (پیشنهادی)**
1. فایل `create-icons.html` را در مرورگر باز کنید
2. روی دکمه‌های "دانلود آیکون" کلیک کنید
3. فایل‌های `icon-192.png` و `icon-512.png` را در root پروژه ذخیره کنید

**روش 2: استفاده از Node.js**
```bash
npm install canvas
npm run generate-icons
```

### 2. تنظیم Repository

1. یک repository جدید در GitHub ایجاد کنید
2. کد را push کنید:
```bash
git init
git add .
git commit -m "Initial commit"
git branch -M main
git remote add origin https://github.com/yourusername/cat-food-reminder-pwa.git
git push -u origin main
```

### 3. تنظیم GitHub Pages

**روش 1: استفاده از GitHub Actions (پیشنهادی)**

1. به Settings > Pages بروید
2. در بخش "Source"، "GitHub Actions" را انتخاب کنید
3. فایل `.github/workflows/deploy.yml` از قبل ایجاد شده است
4. با هر push به branch `main`، اپلیکیشن به صورت خودکار دیپلوی می‌شود

**روش 2: دیپلوی دستی**

1. به Settings > Pages بروید
2. در بخش "Source":
   - Branch: `main` یا `master` را انتخاب کنید
   - Folder: `/ (root)` را انتخاب کنید
3. روی Save کلیک کنید

### 4. تنظیم مسیرها در manifest.json

اگر نام repository شما متفاوت از `cat-food-reminder-pwa` است:

1. فایل `manifest.json` را باز کنید
2. `start_url` و `scope` را تغییر دهید:
```json
{
  "start_url": "/your-repo-name/",
  "scope": "/your-repo-name/"
}
```

**نکته:** اگر repository شما `username.github.io` است (root deployment):
```json
{
  "start_url": "/",
  "scope": "/"
}
```

### 5. فعال‌سازی GitHub Pages

پس از تنظیمات، GitHub Pages به صورت خودکار فعال می‌شود و اپلیکیشن در آدرس زیر در دسترس خواهد بود:

```
https://yourusername.github.io/cat-food-reminder-pwa/
```

## تست محلی

قبل از دیپلوی، می‌توانید اپلیکیشن را به صورت محلی تست کنید:

```bash
# نصب http-server (اگر نصب نشده)
npm install -g http-server

# اجرای سرور محلی
npm start
# یا
npx http-server -p 8000 -c-1
```

سپس به `http://localhost:8000` بروید.

## عیب‌یابی

### Service Worker کار نمی‌کند
- مطمئن شوید که از HTTPS استفاده می‌کنید (GitHub Pages به صورت خودکار HTTPS دارد)
- در Developer Tools > Application > Service Workers بررسی کنید

### آیکون‌ها نمایش داده نمی‌شوند
- مطمئن شوید که فایل‌های `icon-192.png` و `icon-512.png` در root پروژه وجود دارند
- مسیر آیکون‌ها در `manifest.json` را بررسی کنید

### PWA نصب نمی‌شود
- مطمئن شوید که `manifest.json` معتبر است
- Service Worker باید به درستی ثبت شده باشد
- در Developer Tools > Application > Manifest بررسی کنید

## به‌روزرسانی

برای به‌روزرسانی اپلیکیشن:

1. تغییرات را commit کنید
2. به GitHub push کنید
3. GitHub Actions به صورت خودکار دیپلوی می‌کند
4. برای به‌روزرسانی Service Worker، کاربران باید صفحه را refresh کنند

## نکات مهم

- ⚠️ **HTTPS ضروری است**: PWA فقط روی HTTPS کار می‌کند (GitHub Pages به صورت خودکار HTTPS دارد)
- 📱 **تست روی موبایل**: حتماً اپلیکیشن را روی موبایل تست کنید
- 🔄 **Cache**: بعد از تغییرات، ممکن است نیاز به Clear Cache باشد
- 📊 **Analytics**: می‌توانید Google Analytics اضافه کنید (اختیاری)

